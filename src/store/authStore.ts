// The auth store. The provider and hook are implemented in AuthContext.tsx
// (it needs JSX); this is the module the rest of the app imports them from.
export { AuthProvider, useAuth } from './AuthContext';
